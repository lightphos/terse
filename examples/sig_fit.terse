

Point { x: i64, y: i64 }

sig Printable {
  fn str() -> str
}

sig Coordinate {
  fn cord() -> i64
}

fit Point as Printable, Coordinate {
  fn show() -> i64 { pr(self.x + " " + self.y)  }
  fn add() -> i64 { self.x + self.y }
  fn str() -> str {
    "Point { x: " + self.x + ", y: " + self.y + " }"
  }
  fn cord() -> i64 {
    self.x + self.y
  }
}

fn print(p: Printable) {
  pr("poly " + p.str())
}

fn main() -> i64 { 
    let p = Point {x: 3, y: 2};
    p.show()
    print(p)
    pr(p.add())
    pr(" -> " + p.cord())
    p.x
 }
